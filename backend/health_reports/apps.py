from django.apps import AppConfig

class HealthReportsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'health_reports'
    verbose_name = 'Health Reports Storage'
