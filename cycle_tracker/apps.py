from django.apps import AppConfig

class CycleTrackerConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'cycle_tracker'
    verbose_name = 'Menstrual Cycle Tracker'
